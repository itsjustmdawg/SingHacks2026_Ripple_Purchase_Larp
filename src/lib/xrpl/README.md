# XRPL module

Owned by the XRPL / Transaction workstream. This module will construct, sign,
submit, and verify payments only after policy approval. It currently defines
interfaces and throws explicit not-implemented errors. It never creates or
stores wallet secrets.
